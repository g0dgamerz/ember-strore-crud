import Route from '@ember/routing/route';
// import { inject } from '@ember/service';

// eslint-disable-next-line ember/no-classic-classes
export default Route.extend({
  // ajax: inject.service(),
  // model() {
  //   return this.ajax.request('/product');
  // },
});
